# Linuxサーバー接続先（deploy/deploy.sh が使う）
HA_SERVER=quo@192.168.1.2
HA_REMOTE_DIR=/srv/homeassistant

# コンテナランタイム（任意、デフォルト "docker compose"）
# Podman を使うなら network_mode: host のため rootful 推奨:
#   COMPOSE_CMD="sudo podman compose"
# COMPOSE_CMD=docker compose
